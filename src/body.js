import codefactory from './codefactory'

export default function body() {
  codefactory('body', 'main', 'container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center', 'Body bro', 'content');
}